!!!
%html
  %head
    %title ThunderLight
  %body
    %h1 Thunderlight
    %form{action: '/new_board', method: 'POST'}
      %input{type: 'text', name: 'title', class: 'text'}
      %input{type: 'submit', value: 'New Board'}
    - boards.each do |board|
      %ul
        %li
          %a{href: "/board/#{board.id}"} #{board.title || board.id}
